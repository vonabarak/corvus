[Unit]
Description=Corvus VM Management Daemon
After=network.target

[Service]
Type=exec
ExecStart=%h/.local/bin/corvus \
    --host 127.0.0.1 \
    --port 9876 \
    --database postgresql://localhost/corvus

Restart=on-failure
RestartSec=5

[Install]
WantedBy=default.target
